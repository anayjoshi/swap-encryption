to compile, type

make

usage:

./swap source dst

the nibbles of every byte contained in 'source' are swapped and the result is stored in the dst
example,
./swap example.txt encrypted.txt
./swap encrypted.txt example_2.txt

example.txt and example_2.txt would be exactly the same
